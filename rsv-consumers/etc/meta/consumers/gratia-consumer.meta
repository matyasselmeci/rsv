[gratia-consumer]

# The HOME=/tmp declaration is a workaround in Gratia the Chris Green indicated
# that we need in order to successfully send Gratia records.  Removing it result
# in a hard-to-debug error.
environment = HOME=/tmp;

# We used to add the following in the environment statement.  We shouldn't need
# it anymore but we might need to change how the gratia-consumer imports Gratia
# PYTHONPATH=!!VDT_LOCATION!!/gratia/probe/common:!!VDT_LOCATION!!/gratia/probe/metric
