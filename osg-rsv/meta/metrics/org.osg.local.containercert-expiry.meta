[org.osg.local.containercert-expiry]
default-cron-interval = 12 1 * * *

execute = local
service-type = OSG-Local-Monitor
output-format = wlcg
probe-spec = v3
need-proxy = false

[org.osg.local.containercert-expiry env]
PERL5LIB = PREPEND | !!VDT_LOCATION!!/osg-rsv/bin/probes:!!VDT_PERL5LIB!!
